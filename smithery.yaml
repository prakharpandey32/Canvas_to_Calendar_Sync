name: canvas_to_calendar_sync
version: 1

start:
  command: python
  args: ["server.py"]
  env:
    MCP_TRANSPORT: "http"   # your server switches to HTTP mode
    PORT: "8000"            # http server port

http:
  port: 8000
  mcpEndpoint: /mcp
